/**
 * Application configuration
 * Add your app-level configuration here
 */

export const config = {
  app: {
    name: 'Todo List',
    description: 'Built with Onion Architecture',
  },
  // Add more configuration as needed
};

export default config;
